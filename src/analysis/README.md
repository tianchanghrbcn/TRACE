# Analysis

This directory contains the artifact-side analysis code.

It will read the original pipeline outputs and/or standardized raw tables,
then generate processed tables and figures for the paper.

Some scripts may both process data and generate figures; we do not force
a strict split between data processing and plotting.
