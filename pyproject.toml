[project]
name = "trace-artifact"
version = "0.0.0"
description = "Reproducibility package for TRACE."
readme = "README.md"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = []

[tool.ruff]
line-length = 100
target-version = "py310"
